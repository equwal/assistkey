<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug builds only. The reader's e-ink panel is driven outside
    SurfaceFlinger, so `adb shell screencap` returns a black frame; ShotApp has
    each screen draw its own view tree to a PNG instead. None of this is in a
    release build.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:name=".debug.ShotApp">
        <!-- Test hook: adb shell am broadcast -n <pkg>/dev.equwal.assistkey.debug.InsertReceiver -e text "..." -->
        <receiver android:name=".debug.InsertReceiver" android:exported="true" />
        <!-- A recognizer that hears a fixed sentence, so voice typing can be tested with no voice. -->
        <service android:name=".debug.FakeRecognitionService" android:exported="true" android:label="Test bench recognizer">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
            </intent-filter>
        </service>
    </application>
</manifest>
