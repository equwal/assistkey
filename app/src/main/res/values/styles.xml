<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        The assist activity must never be seen. Translucent, no title, no window
        animation - otherwise you get a visible flash every time you long-press power.
    -->
    <style name="Theme.Invisible" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
