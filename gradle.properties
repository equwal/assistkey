org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Release identity. versionCode must rise with every upload to Play.
assistkey.versionName=0.1.1-beta
assistkey.versionCode=17

# The free beta ends on this date (UTC) at the latest. It is the backstop for
# installs that cannot reach Google Play; the real switch is the
# assistkey_beta_open product in Play Console - see play/CHECKLIST.md.
assistkey.betaExpires=2027-03-31
